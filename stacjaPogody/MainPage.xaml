<Page
    x:Class="stacjaPogody.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:stacjaPogody"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Loaded="Grid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Stacja Pogody" FontSize="50" Margin="5" HorizontalAlignment="Center" Grid.Row="0" />
        <Button x:Name="admin" Content="⌸" HorizontalAlignment="Left" Margin="5" Click="admin_Click" FontSize="20" Grid.Row="0"/>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="5">
            <TextBlock Text="Dane w tej chwili" FontSize="30" Margin="5" />
            <TextBlock x:Name="dane" Margin="5" FontSize="25" />
        </StackPanel>

    </Grid>
</Page>
